# Tasks

- Maintain route and page behavior.
- Convert role landing pages into workflows.
- Validate auth response handling.
- Plan CRA modernization.

## Active Task: Cygnus UI Gap Closure

Status: assigned.

Scope:

- Audit and improve `services/cygnus` public routes, auth routes, role routes, shared header/footer, CSS, responsive behavior, and accessibility.

Priority sequence:

1. Fix broken links and inert CTAs.
2. Stabilize shared layout and accessibility.
3. Improve auth and role onboarding UX states.
4. Convert investor, partner, and provider pages into actionable dashboard entry screens.
5. Scope global CSS and add responsive polish.

Evidence and report:

- `reports/CYGNUS_UI_GAP_AUDIT.md`
